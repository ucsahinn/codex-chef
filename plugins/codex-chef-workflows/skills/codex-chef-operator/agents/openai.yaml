interface:
  display_name: "Chef Operator"
  short_description: "Maintain Codex Chef safely."
  default_prompt: "Use $codex-chef-operator to audit and improve this Codex Chef setup without weakening safety gates."

policy:
  allow_implicit_invocation: true
